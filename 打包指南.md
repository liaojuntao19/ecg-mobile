# 云打包 APK 操作指南（GitHub Actions）

> 目标：把 `ecg_mobile_app` 文件夹推到 GitHub，云端自动打包成 APK，下载装手机

---

## 一、准备：确认 ecg_mobile_app 文件夹内容

```
ecg_mobile_app/
├── main.py              ← 手机 App 主程序（Kivy，复用 ecg_filter.py）
├── ecg_filter.py        ← 你的滤波算法（原封不动）
├── buildozer.spec       ← 打包配置（应用名/权限/依赖）
└── .github/workflows/build.yml  ← GitHub 自动打包配置
```

## 二、上传 GitHub（网页操作，不需要装 Git 客户端）

1. 登录 GitHub → 右上角 `+` → **New repository**
2. 仓库名填 `ecg-mobile`（随意）→ 选 **Public**（免费）→ Create
3. 进入仓库 → 点 **"uploading an existing file"**
4. 把 `ecg_mobile_app` 文件夹里的**所有文件**拖进去（包括 `.github` 文件夹！）
   - ⚠️ `.github` 是隐藏文件夹，网页上传时可能不显示——**用"Add file → Upload files"拖整个文件夹**，或在电脑上把 `.github` 一起拖
5. 写个说明 → Commit changes

## 三、自动打包

1. 上传后，GitHub 自动触发 Actions 打包
2. 仓库页点 **Actions** 标签 → 看到 `Build APK` 在跑（转圈）
3. **等待 20-60 分钟**（首次要下载 Android SDK，较久）

## 四、下载 APK

1. Actions 跑完（绿色 ✓）→ 点进那次运行
2. 底部 **Artifacts** → 下载 `ecg-apk`（zip）
3. 解压得到 `.apk` 文件

## 五、装手机

1. APK 传到手机（微信/数据线/网盘）
2. 手机设置 → 安全 → **允许安装未知来源应用**
3. 点 APK 安装 → 打开「心电ECG」App

## ⚠️ 常见问题

| 问题 | 解决 |
|---|---|
| Actions 报错 | 把报错截图发我，我改配置 |
| 打包慢/超时 | 首次正常（下载 SDK），重跑一次 |
| APK 很大（~100MB） | 正常（含 scipy），以后可优化 |
| 手机打开闪退 | 日志在手机 `logcat`，告诉我现象 |
| 中文乱码 | 手机缺中文字体，我后面打包中文字体进去 |

## 📌 提醒

- 手机打开 App 后：选「BLE连接」→ 点开始 → 自动扫描 ECG-S3 → 连接 → 波形
- 手机蓝牙要先打开
- 打包后如果要改 UI：改 `main.py` → 重新 push → 自动重新打包
